{
  "name": "@rhinolabs/rhinolabs-ui",
  "private": true,
  "scripts": {
    "build": "turbo build",
    "build-storybook": "pnpm --filter=@rhino-ui/components build-storybook",
    "dev": "turbo dev",
    "eslint": "turbo eslint",
    "stylelint": "turbo stylelint",
    "clean:node-modules": "rimraf packages/**/node_modules && rimraf libs/**/node_modules && rm -rf node_modules"
  },
  "devDependencies": {
    "rimraf": "^5.0.5",
    "ts-loader": "^8.4.0",
    "tslib": "^2.6.2",
    "turbo": "latest",
    "typescript": "^5.3.3"
  },
  "packageManager": "pnpm@8.9.0",
  "engines": {
    "node": ">=18"
  }
}
